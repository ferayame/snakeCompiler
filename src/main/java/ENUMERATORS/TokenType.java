package ENUMERATORS;

public enum TokenType {
    Snk_Begin, Snk_End, Snk_Int, Snk_Real, Snk_Strg, Snk_Print,
    IDENTIFIER, INTEGER, FLOAT, RELOPERATOR, COMMENT, EndOfInstruction, COMMA,
    IfElse, OpenSBraquets, ClosedSBraquets, BEGIN, END,
    SET, GET, MESSAGE, UNKNOWN, FROM
}
